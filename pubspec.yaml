name: learntounsi_mobile
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Firebase
  firebase_core: ^3.7.0
  firebase_auth: ^5.1.0
  cloud_firestore: ^5.0.0
  firebase_storage: ^12.1.0   # ✅ AJOUT IMPORTANT

  # State Management
  provider: ^6.1.5+1

  file_picker: ^8.0.3

  # HTTP
  http: ^1.2.2
  url_launcher: ^6.3.2

  # PDF viewer
  flutter_pdfview: ^1.2.7
  path_provider: ^2.1.1

  # WebView
  webview_flutter: ^4.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/data/allquiz.json   # ✅ corrigé, pas de caractère spécial
